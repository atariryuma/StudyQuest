{
  "name": "studyquest",
  "version": "1.0.247",
  "private": true,
  "scripts": {
    "deploy": "clasp push --force --no-open",
    "test": "node scripts/check-deps.js && jest",
    "postversion": "node scripts/postversion.js"
  },
  "devDependencies": {
    "@google/clasp": "^2.4.2",
    "jest": "^29.7.0",
    "jsdom": "^22.1.0"
  }
}
