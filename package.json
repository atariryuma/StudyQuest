{
  "name": "studyquest",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "deploy": "clasp push --force --no-open",
    "test": "jest"
  },
  "devDependencies": {
    "@google/clasp": "^2.4.2",
    "jest": "^29.7.0"
  }
}
